[pytest]
testpaths = tests
asyncio_mode = auto

# Test reporting options
addopts =
    -v
    --cov=.
    --cov-report=html
    --cov-report=xml
    --cov-report=term
    --junit-xml=test-results/junit.xml