# Citation Generator - Environment Variables
# Copy this file to .env and customize for your local development

# Database Configuration - REQUIRED
# This connects to the PostgreSQL container defined in docker-compose.yml
# For local development with Docker: postgresql://postgres:postgres@localhost:5432/citation_generator_db
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/citation_generator_db

# PostgreSQL Configuration (used by docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=citation_generator_db

# Environment
ENVIRONMENT=development

# CORS Configuration (optional)
# For local development, leave unset to allow all origins
# For production, set to your frontend URL (comma-separated for multiple)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Note: Production deployment uses Azure App Service environment variables
# - DATABASE_URL: Set in Azure App Service Configuration
# - ALLOWED_ORIGINS: Set automatically by CD pipeline
# - ENVIRONMENT: Set to "production" in Azure
